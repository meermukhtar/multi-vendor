import 'package:flutter/material.dart';

class FavouritesProducts extends StatelessWidget {
  const FavouritesProducts({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("favo"),),
      body: Center(child: Text("comming"),),
    );
  }
}
